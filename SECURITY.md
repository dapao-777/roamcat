# 安全政策 / Security Policy

## 支持的版本

| 版本 | 状态 |
|---|---|
| `main` 分支（当前 0.0.1 prerelease） | 接受安全修复 |
| 已发布的商店/Release 版本 | 仅最新版本接受安全修复 |

本项目尚未上架 Chrome Web Store。预发布阶段的安全修复会直接落在 `main` 分支，不单独回溯旧版。

## 为什么这个项目对安全问题特别敏感

RoamCat 的攻击面比一般扩展大，发现以下类别的问题请优先私下报告：

- **凭据外泄**：自备 API 的密钥、连接器会话状态被发送到非预期源（页面、第三方域、日志）。
- **信任边界穿透**：网页脚本触达仅允许扩展上下文使用的消息类型；`extensionOrigin` 校验绕过；非扩展 sender 冒充。
- **本机连接器**：`connector/` 的 native messaging 帧解析、子进程调用、登录 URL 白名单、诊断文件权限。
- **提示注入落地**：模型输出未经规范化直接渲染进页面 DOM，或触发超出声明范围的行为。
- **隐私数据外泄**：阅读历史、正文内容、URL 出现在诊断/日志/遥测中。

## 报告方式

**请勿在公开 Issue 中描述可复现的漏洞细节。**

- 首选：GitHub 仓库的 **Private Vulnerability Reporting**（Security → Report a vulnerability）。
- 备用：发邮件到 **777dapao@gmail.com**，主题前缀 `[RoamCat Security]`。

报告请尽量包含：受影响文件/函数、复现步骤、影响范围、你判断的等级。允许附带 PoC，但请不要在真实用户数据上验证。

## 响应承诺

- 确认收到：3 天内。
- 初步评估与等级判定：7 天内。
- 修复目标：高危问题优先于一切其他工作；确认后尽快发版并在 Release Notes 中致谢（可匿名）。

## 范围之外

- 需要你本机已安装的第三方 CLI（`codex` / `grok` / `agy`）自身的漏洞——请报告给对应厂商。
- 需要你已配置的自备 API 服务商（OpenAI、Anthropic 等）的数据处理行为——本项目只在请求时发送你选定的文本，服务商侧策略由其隐私条款约束。
- 依赖库（Transformers.js、ONNX Runtime Web）的上游漏洞——请同步报告给上游，同时欢迎通知本项目跟进升级。
